{
  "name": "tavern-director",
  "version": "1.0.0",
  "description": "酒馆导演插件 —— 数据适配层 + 导演调度层 + 前端UI壳子",
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "build:watch": "rollup -c -w",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^11.1.6",
    "rollup": "^4.18.0",
    "tslib": "^2.6.0",
    "typescript": "^5.4.0"
  }
}
